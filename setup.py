from setuptools import find_packages, setup

setup(
    name='netbox-asset-tag-be-gone',
    version='0.1',
    description='NetBox Asset Tag Be Gone',
    install_requires=[],
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)